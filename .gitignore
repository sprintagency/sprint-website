/node_modules
/.next
/out
/build
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env*
.vercel
*.tsbuildinfo
next-env.d.ts

# MCP config holds a personal API key (Context7) — keep the real file local,
# commit only .mcp.json.example. API keys are per-account, never share them.
.mcp.json
